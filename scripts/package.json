{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "verify-payment-allocation": "tsx ./src/verify-payment-allocation.ts",
    "verify-vendor-payment-ledger-side": "tsx ./src/verify-vendor-payment-ledger-side.ts",
    "backfill-vendor-payment-ledger-side": "tsx ./src/backfill-vendor-payment-ledger-side.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@types/pg": "^8.20.0",
    "tsx": "catalog:"
  }
}
